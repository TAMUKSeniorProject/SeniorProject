<%= link_to discussion_path(notification.notifiable.id), class: "dropdown-item #{"unread" if !notification.read_at?}" do %>
  <%= notification.actor.username %>
  <%= notification.action %>
  on <%= notification.notifiable_type.underscore.humanize.downcase %> <%= notification.notifiable.title %>
<% end %>