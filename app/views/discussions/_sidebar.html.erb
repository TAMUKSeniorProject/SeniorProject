<h2>Categories</h2>
<ul>
    <% @channels.each do |c| %>
    <li><%= link_to c.channel, c %></li>
    <% end %>
</ul>

<br/>
<% if has_role?(:admin) %>
    <%= link_to 'New Channel', new_channel_path, class:"btn btn-info" %>
<% end %>